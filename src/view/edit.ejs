<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta http-equiv="X-UA-Compatible" content="IE=edge">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Cotizaciones</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</head>
<body>
    <% if (results) { %>
    <div class="container mt-4">
        <div class="card border-secondary">
        <div class="card-header">Editar Registro</div>
        <div class="card-body">
            <form action="/UpdateProducts" method ="POST">
                <input hidden name="IDCOTIZACION" id="IDCOTIZACION" value="<%= results.IDCOTIZACION %>"> 
                <div class="mb-3">
                    <label for="user" class="form-label">Nombre</label>
                    <input type="text" class="form-control" id="NOMBRECOMPLETO" name="NOMBRECOMPLETO" value="<%= results.NOMBRECOMPLETO %>"  tabindex="1">
                </div>
                <div class="mb-3">
                    <label for="user" class="form-label">Tipo Cotizacion</label>
                    <select id="TIPOCOTIZACION" name="TIPOCOTIZACION" class="form-select" aria-label="Default select example">
                        <option <%= results.TIPOCOTIZACION=='OFICINAS Y LOCALES' ? "selected" : "" %> value="OFICINAS Y LOCALES" >OFICINAS Y LOCALES</option>
                        <option <%= results.TIPOCOTIZACION=='STAND' ? "selected" : "" %> value="STAND">STAND</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label for="user" class="form-label">Estado</label>
                    <select id="ESTADO" name="ESTADO" class="form-select" aria-label="Default select example">
                        <option <%= results.ESTADO=='PENDIENTE' ? "selected" : "" %> value="PENDIENTE">PENDIENTE</option>
                        <option <%= results.ESTADO=='CONFIRMADO' ? "selected" : "" %> value="CONFIRMADO">CONFIRMADO</option>
                    </select>
                </div>
                <button type="submit" class="btn btn-primary" tabindex="5">Guardar</button>
                <a href="/products" class="btn btn-secondary">Cancelar</a>
            </form>
        </div>
        </div>
    </div>
    <% } %>
</body>
</html>